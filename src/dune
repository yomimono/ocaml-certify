(library
  (name certify_cli)
  (modules common)
  (wrapped false)
  (libraries mirage-crypto mirage-crypto-pk mirage-crypto-rng.unix cmdliner cstruct x509 ptime ptime.clock.os))

(executable
  (name certify)
  (public_name certify)
  (modules certify csr sign selfsign)
  (libraries certify_cli))
